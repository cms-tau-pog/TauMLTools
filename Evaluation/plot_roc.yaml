# input
path_to_mlflow: ???
experiment_name: ???
discriminators: # in the format -> run_id: ['curve_type_1', 'curve_type_2', ...]
  ???: ['roc_curve', 'roc_wp']
  ???: ['roc_wp']
reference: 
  ???: 'roc_curve'

# region
vs_type: ??? # jet/e/mu/tau
sample_tau: ??? # dataset alias 
sample_vs_type: ??? # dataset alias 
pt_bin: ??? # list, e.g [20, 100] 

# will log the plot to the run of the first discriminator in the "discriminators" list
output_name: 'roc_curve_vs_${vs_type}_pt_${pt_bin[0]}_${pt_bin[1]}'